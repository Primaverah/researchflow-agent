# 工具调用、参数校验与安全

在 Agent 系统中，tool calling（工具调用）需要稳定的名称、用途说明和参数模型。调用开始前，Pydantic 可以检查必填字段、字段类型、取值范围以及多余参数，避免无效输入进入实际执行逻辑。

工具注册表负责检测重复名称，并在请求未知工具时返回明确错误。工具执行结果应区分成功和可预期失败，让 Agent 可以根据错误类型决定继续、跳过还是停止。

文件工具必须重视安全。读取文档或保存研究笔记时，需要拒绝绝对路径、父目录穿越和符号链接逃逸，并确保最终路径仍位于允许的根目录中。
