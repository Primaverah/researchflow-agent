# 文档检索系统

关键词检索通过查询词在标题和正文中的出现情况计算相关性。它实现简单、结果可解释，适合小规模本地文档，也便于为 Agent 提供稳定、可测试的搜索结果。

语义检索通常使用向量表示寻找含义相近的内容，即使原文没有出现完全相同的关键词也可能命中。混合检索会结合关键词分数和语义分数，在召回率与精确度之间取得平衡。

离线第一版可以先采用关键词检索，并对标题匹配赋予更高权重。以后替换搜索后端时，工具调用参数和结果格式可以保持不变，从而减少 Agent 层的改动。
