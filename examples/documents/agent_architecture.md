# Agent 架构与执行状态

一个可靠的 Agent 通常由状态、计划和执行循环组成。状态记录当前研究问题、已经完成的步骤、工具调用结果以及停止原因，使每一次决策都可以被检查和复现。

计划把复杂任务拆分为顺序明确的小步骤。执行器读取当前状态，选择下一步动作，再把执行结果写回状态。第一版系统可以使用确定性规则完成这一过程，而不依赖在线模型。

工具调用是 Agent 与外部能力之间的边界。执行器不应直接读写文档，而应通过经过注册和参数校验的工具访问数据。清晰的边界有助于记录执行轨迹、处理失败，并保证本地文件访问安全。
